flask>=3.0.0
xlrd>=2.0.1
openpyxl>=3.1.2
pdfplumber>=0.11.0
python-docx>=1.1.0
gunicorn>=21.2.0
reportlab>=4.0.0

# OCR de PDF escaneado (opcional) — a aba Implantacao funciona sem, avisando
# que o arquivo precisa de OCR. Para habilitar, instale tambem o binario:
#   apt-get install -y tesseract-ocr tesseract-ocr-por
# pymupdf>=1.24.0
# pytesseract>=0.3.10
